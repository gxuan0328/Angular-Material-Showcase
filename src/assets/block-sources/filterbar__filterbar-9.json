{
  "category": "filterbar",
  "variant": "filterbar-9",
  "files": {
    "filterbar-9.component.html": "<!--\n\tInstalled from https://ui.angular-material.dev/\n\tUpdate this file using `npx @ngm-dev/cli update filterbar/filterbar-9`\n-->\n\n<div class=\"relative p-4\">\n  <div\n    class=\"flex h-48 items-start justify-start gap-x-2 rounded-md border border-outline-variant p-6 shadow-sm sm:justify-end\"\n  >\n    <!-- Trigger button with cdkOverlayOrigin -->\n    <button\n      matButton=\"outlined\"\n      cdkOverlayOrigin\n      #trigger=\"cdkOverlayOrigin\"\n      (click)=\"toggleOverlay()\"\n    >\n      <mat-icon>tune</mat-icon>\n      Display\n    </button>\n\n    <!-- Connected overlay with mat-card -->\n    <ng-template\n      cdkConnectedOverlay\n      [cdkConnectedOverlayOrigin]=\"trigger\"\n      [cdkConnectedOverlayOpen]=\"isOpen\"\n      [cdkConnectedOverlayWidth]=\"280\"\n      [cdkConnectedOverlayPositions]=\"STANDARD_DROPDOWN_BELOW_POSITIONS\"\n      [cdkConnectedOverlayHasBackdrop]=\"true\"\n      [cdkConnectedOverlayBackdropClass]=\"'cdk-overlay-transparent-backdrop'\"\n      (backdropClick)=\"closeOverlay()\"\n      (positionChange)=\"onPositionChange($event)\"\n    >\n      <mat-card\n        [class.display-properties-animations-disabled]=\"_animationsDisabled\"\n        [class]=\"animationClass()\"\n        [class.display-properties-animating]=\"isAnimating()\"\n        (animationend)=\"onAnimationDone($event.animationName)\"\n        (animationcancel)=\"onAnimationDone($event.animationName)\"\n        (animationstart)=\"onAnimationStart($event.animationName)\"\n      >\n        <mat-card-header>\n          <mat-card-subtitle>Display properties</mat-card-subtitle>\n        </mat-card-header>\n        <mat-card-content>\n          <mat-chip-listbox\n            multiple\n            aria-label=\"Display properties\"\n            class=\"flex flex-wrap gap-2 mt-2\"\n          >\n            @for (prop of propertyKeys; track prop) {\n            <mat-chip-option\n              [selected]=\"displayProperties[prop]\"\n              (selectionChange)=\"toggleProperty(prop, $event)\"\n              class=\"text-xs\"\n            >\n              {{ prop }}\n            </mat-chip-option>\n            }\n          </mat-chip-listbox>\n        </mat-card-content>\n      </mat-card>\n    </ng-template>\n  </div>\n  <!-- gradient for demo purpose -->\n  <div\n    class=\"absolute inset-x-0 bottom-0 -mb-1 h-14 rounded-b-md bg-gradient-to-t from-surface via-surface to-transparent\"\n  ></div>\n</div>\n",
    "filterbar-9.component.scss": "/*\n\tInstalled from https://ui.angular-material.dev/\n\tUpdate this file using `npx @ngm-dev/cli update filterbar/filterbar-9`\n*/\n\n// Animation keyframes\n@mixin fade-keyframes($direction, $type) {\n  $start-y: if($direction == 'up', 8px, -8px);\n  $end-y: if($type == 'in', 0, if($direction == 'up', -8px, 8px));\n  $start-opacity: if($type == 'in', 0, 1);\n  $end-opacity: if($type == 'in', 1, 0);\n\n  @keyframes display-properties-fade-#{$type}-#{$direction} {\n    #{if($type == 'in', '0%', 'from')} {\n      opacity: $start-opacity;\n      transform: translateY(if($type == 'in', $start-y, 0));\n    }\n    #{if($type == 'in', '100%', 'to')} {\n      opacity: $end-opacity;\n      transform: translateY($end-y);\n    }\n  }\n}\n\n// Generate all keyframes\n@include fade-keyframes('up', 'in');\n@include fade-keyframes('down', 'in');\n@include fade-keyframes('up', 'out');\n@include fade-keyframes('down', 'out');\n\n// Animation classes\n$animation-timing: cubic-bezier(0, 0, 0.2, 1);\n\n%animation-base {\n  animation-duration: 120ms;\n  animation-timing-function: $animation-timing;\n}\n\n%animation-out {\n  @extend %animation-base;\n  animation-duration: 100ms;\n  animation-delay: 25ms;\n  animation-timing-function: linear;\n  animation-fill-mode: forwards;\n}\n\n.animate-display-properties {\n  &-fade-in-up {\n    @extend %animation-base;\n    animation-name: display-properties-fade-in-up;\n  }\n\n  &-fade-in-down {\n    @extend %animation-base;\n    animation-name: display-properties-fade-in-down;\n  }\n\n  &-fade-out-up {\n    @extend %animation-out;\n    animation-name: display-properties-fade-out-up;\n  }\n\n  &-fade-out-down {\n    @extend %animation-out;\n    animation-name: display-properties-fade-out-down;\n  }\n}\n\n.display-properties-animations-disabled {\n  animation: none;\n}\n\n.display-properties-animating {\n  pointer-events: none;\n}\n",
    "filterbar-9.component.ts": "/*\n\tInstalled from https://ui.angular-material.dev/\n\tUpdate this file using `npx @ngm-dev/cli update filterbar/filterbar-9`\n*/\n\nimport {\n  ANIMATION_MODULE_TYPE,\n  Component,\n  computed,\n  effect,\n  inject,\n  signal,\n  ViewEncapsulation,\n} from '@angular/core';\n\nimport { FormsModule } from '@angular/forms';\nimport { MatButtonModule } from '@angular/material/button';\nimport { MatIconModule } from '@angular/material/icon';\nimport { MatCardModule } from '@angular/material/card';\nimport {\n  MatChipsModule,\n  MatChipSelectionChange,\n} from '@angular/material/chips';\nimport {\n  ConnectedOverlayPositionChange,\n  OverlayModule,\n  STANDARD_DROPDOWN_BELOW_POSITIONS,\n} from '@angular/cdk/overlay';\n\n// Define property keys type\ntype PropertyKey =\n  | 'item_id'\n  | 'created_at'\n  | 'updated_at'\n  | 'type'\n  | 'tags'\n  | 'status';\n\n/** Name of the enter animation `@keyframes`. */\nconst ENTER_ANIMATIONS = [\n  'display-properties-fade-in-up',\n  'display-properties-fade-in-down',\n];\n\n/** Name of the exit animation `@keyframes`. */\nconst EXIT_ANIMATIONS = [\n  'display-properties-fade-out-up',\n  'display-properties-fade-out-down',\n];\n\n@Component({\n  selector: 'ngm-dev-block-filterbar-9',\n  templateUrl: './filterbar-9.component.html',\n  styleUrls: ['./filterbar-9.component.scss'],\n  imports: [\n    FormsModule,\n    MatButtonModule,\n    MatIconModule,\n    MatCardModule,\n    MatChipsModule,\n    OverlayModule,\n  ],\n  // Disable view encapsulation so that animations are not scoped to the component\n  encapsulation: ViewEncapsulation.None,\n})\nexport class Filterbar9Component {\n  // Display properties state\n  displayProperties: Record<PropertyKey, boolean> = {\n    item_id: true,\n    created_at: true,\n    updated_at: false,\n    type: false,\n    tags: true,\n    status: true,\n  };\n\n  // Property keys for iteration in template\n  propertyKeys: PropertyKey[] = [\n    'item_id',\n    'created_at',\n    'updated_at',\n    'type',\n    'tags',\n    'status',\n  ];\n\n  isAnimating = signal(false);\n  isSoftOpen = signal(false);\n  // Overlay state\n  isOpen = false;\n\n  animationDirection = signal<'up' | 'down'>('down');\n\n  animationClass = computed(() => {\n    const direction = this.animationDirection();\n    const isOpen = this.isSoftOpen();\n    return isOpen\n      ? direction === 'up'\n        ? 'animate-display-properties-fade-in-up'\n        : 'animate-display-properties-fade-in-down'\n      : direction === 'up'\n        ? 'animate-display-properties-fade-out-down'\n        : 'animate-display-properties-fade-out-up';\n  });\n\n  readonly _animationsDisabled =\n    inject(ANIMATION_MODULE_TYPE, { optional: true }) === 'NoopAnimations';\n\n  readonly STANDARD_DROPDOWN_BELOW_POSITIONS =\n    STANDARD_DROPDOWN_BELOW_POSITIONS.map((item) => ({\n      ...item,\n      offsetY: item.overlayY === 'bottom' ? -8 : 8,\n    }));\n\n  constructor() {\n    effect(() => {\n      const isSoftOpen = this.isSoftOpen();\n      if (isSoftOpen) {\n        this.isOpen = true;\n      } else {\n        if (this._animationsDisabled) {\n          this.isOpen = false;\n        }\n      }\n    });\n  }\n\n  // Toggle overlay\n  toggleOverlay(): void {\n    this.isSoftOpen.update((value) => !value);\n  }\n\n  // Close overlay\n  closeOverlay(): void {\n    this.isSoftOpen.set(false);\n  }\n\n  onPositionChange(event: ConnectedOverlayPositionChange): void {\n    this.animationDirection.set(\n      event.connectionPair.overlayY === 'bottom' ? 'up' : 'down',\n    );\n  }\n\n  /** Callback that is invoked when the panel animation completes. */\n  onAnimationDone(state: string) {\n    const isExit = EXIT_ANIMATIONS.includes(state);\n\n    if (isExit) {\n      this.isOpen = false;\n    }\n\n    this.isAnimating.set(false);\n  }\n\n  onAnimationStart(state: string) {\n    this.isAnimating.set(\n      ENTER_ANIMATIONS.includes(state) || EXIT_ANIMATIONS.includes(state),\n    );\n  }\n\n  // Toggle display property\n  toggleProperty(property: PropertyKey, event?: MatChipSelectionChange): void {\n    if (event) {\n      this.displayProperties[property] = event.selected;\n    } else {\n      this.displayProperties[property] = !this.displayProperties[property];\n    }\n  }\n}\n"
  }
}